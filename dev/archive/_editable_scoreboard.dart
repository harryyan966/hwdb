          // final student = students[studentInd];

          // // FINAL SCORE
          // if (assignmentInd == assignments.length) {
          //   final finalScore = finalScores[student.id];
          //   return ScoreCell(score: finalScore);
          // }

          // final assignment = assignments[assignmentInd];
          // final score = scores[student.id]?[assignment.id];



    // if (scoreString == null || assignmentInd == context.read<ScoreBoardCubit>().state.assignments.length) {
